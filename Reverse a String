// Reverse a String 
public class Test2 {

	
	
	public static void main(String args[])
	{
		String str ="ABCD";
		
		String nstr="";
		char ch;
		
		for(int i=0; i<=str.length()-1 ; i++)
		{
			ch= str.charAt(i);
			nstr=ch+nstr;
		}
		System.out.println("resever string "+nstr);
	}
	
}
